# 如何强制更新 Content Script

## ❗ 问题原因

当你重新加载扩展后，**已经打开的网页**仍然使用旧的 `content.js` 文件。

这是因为：
- Content script 在页面加载时注入
- 浏览器会缓存已注入的脚本
- 重新加载扩展不会影响已打开的页面

---

## ✅ 解决方法

### 方法 1：刷新网页（推荐）⭐

**步骤：**
1. 重新加载扩展（在 `chrome://extensions/` 页面点击刷新）
2. **刷新所有已打开的网页**（按 `F5` 或 `Cmd + R`）
3. 按 `Alt + Q` 打开悬浮窗
4. 现在应该看到新的样式

**为什么：** 刷新网页会重新注入最新的 content.js

---

### 方法 2：关闭并重新打开网页

**步骤：**
1. 重新加载扩展
2. **关闭所有相关网页标签**
3. **重新打开一个新的网页**（例如 google.com）
4. 按 `Alt + Q` 打开悬浮窗

---

### 方法 3：清除浏览器缓存

**步骤：**
1. 按 `Ctrl + Shift + Delete`（Mac：`Cmd + Shift + Delete`）
2. 选择"缓存的图片和文件"
3. 时间范围选择"全部时间"
4. 点击"清除数据"
5. 重新加载扩展
6. 刷新网页

---

### 方法 4：禁用并重新启用扩展

**步骤：**
1. 访问 `chrome://extensions/`
2. 找到"即时翻译"扩展
3. 点击开关**禁用**扩展
4. 等待 2 秒
5. 再次点击开关**启用**扩展
6. **刷新所有网页**
7. 按 `Alt + Q` 测试

---

### 方法 5：完全重新安装（终极方法）

**步骤：**
1. 访问 `chrome://extensions/`
2. 点击"移除"删除扩展
3. **关闭所有浏览器窗口**
4. 重新打开浏览器
5. 访问 `chrome://extensions/`
6. 开启"开发者模式"
7. 点击"加载已解压的扩展程序"
8. 选择 `/Users/apple/Desktop/即时翻译` 文件夹
9. **打开一个新的网页**
10. 按 `Alt + Q` 打开悬浮窗

---

## 🔍 如何验证是否生效

### 检查悬浮窗样式

打开悬浮窗后，应该看到：

**✅ 新样式特征：**
- 默认位置：右上角（距离顶部 100px）
- 默认宽度：320px
- Header 高度：44px
- 左侧有拖拽手柄（六圆点图标）
- 引擎下拉框是白色背景，带边框

**❌ 旧样式特征：**
- 默认位置：右下角
- 默认宽度：400px
- Header 高度较大
- 没有独立的拖拽手柄
- 引擎下拉框是灰色背景

---

## 🐛 如果还是没有变化

### 1. 检查 Console 错误

**步骤：**
1. 打开任意网页
2. 按 `F12` 打开开发者工具
3. 切换到 **Console** 标签
4. 按 `Alt + Q` 打开悬浮窗
5. 查看是否有红色错误

**常见错误：**
- `Uncaught ReferenceError: xxx is not defined`
- `Failed to load resource`
- `Cannot read property 'xxx' of null`

如果有错误，请截图告诉我。

---

### 2. 检查 content.js 版本

**步骤：**
1. 打开开发者工具（F12）
2. 切换到 **Sources** 标签
3. 在左侧找到 `content.js`
4. 查看文件内容，搜索 `top: 100px`
5. 如果能找到，说明已更新；如果找不到，说明还是旧版本

---

### 3. 强制清除 Service Worker

**步骤：**
1. 访问 `chrome://extensions/`
2. 找到"即时翻译"扩展
3. 点击扩展卡片上的 **"Service Worker"** 链接
4. 在新打开的页面按 `Ctrl + Shift + Delete`
5. 选择"缓存的图片和文件"
6. 点击"清除数据"
7. 返回扩展页面，重新加载扩展
8. 刷新网页

---

## 📝 正确的测试流程

### 完整步骤：

```
1. 打开 chrome://extensions/
2. 找到"即时翻译"扩展
3. 点击刷新按钮 🔄
4. 关闭所有已打开的网页标签
5. 打开一个新的网页（例如：https://www.google.com）
6. 按 Alt + Q（或 Option + Q）
7. 检查悬浮窗样式
```

### 预期结果：

```
┌─────────────────────────────────┐
│ [⋮⋮] 🌐 即时翻译  [引擎▼] [↔] [×] │
├─────────────────────────────────┤
│                                 │
│       iframe 内容区域            │
│                                 │
└─────────────────────────────────┘
```

- 位置：右上角
- 宽度：320px
- 有拖拽手柄
- 引擎下拉框有边框

---

## 💡 提示

### 开发时的最佳实践

1. **每次修改 content.js 后：**
   - 重新加载扩展
   - 刷新所有测试页面
   - 或打开新的测试页面

2. **使用无痕模式测试：**
   - 无痕模式不会缓存 content script
   - 每次打开都是最新版本

3. **添加版本号：**
   - 在 content.js 开头添加版本号注释
   - 方便确认加载的是哪个版本

4. **使用 Console 日志：**
   ```javascript
   console.log('[JT] Content script loaded, version: 2.0');
   ```
   - 打开 Console 查看版本信息

---

## 🎯 快速验证代码

在 Console 中运行以下代码，检查 content.js 是否已更新：

```javascript
// 检查悬浮窗默认位置
chrome.storage.local.get(['panelX', 'panelY'], (res) => {
  console.log('保存的位置:', res);
});

// 检查宽度状态
chrome.storage.local.get(['widthState'], (res) => {
  console.log('宽度状态:', res.widthState);
});

// 强制清除所有相关缓存
chrome.storage.local.clear(() => {
  console.log('已清除本地缓存，请刷新页面');
});
```

---

## 📞 仍然有问题？

如果尝试了以上所有方法还是不行，请提供：

1. **浏览器版本**
   - 访问 `chrome://version/`
   - 复制版本号

2. **Console 截图**
   - 按 F12 打开开发者工具
   - 切换到 Console 标签
   - 截图错误信息

3. **悬浮窗截图**
   - 按 Alt + Q 打开悬浮窗
   - 截图当前样式

4. **测试步骤**
   - 详细描述你做了什么
   - 期望看到什么
   - 实际看到什么

**文档版本**: v1.0
**更新日期**: 2026-03-11
