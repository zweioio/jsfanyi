# 如何测试插件的新功能

## 🔧 修复内容

1. ✅ **移除旧的 engineSelector 代码** - 避免与新下拉框冲突
2. ✅ **保留引擎下拉框** - 使用新的 `engineSelect` 元素
3. ✅ **侧边栏配置** - manifest.json 已正确配置

---

## 📋 测试步骤

### 1. 重新加载扩展

在 Chrome 浏览器中：

1. 打开扩展管理页面：
   - 地址栏输入：`chrome://extensions/`
   - 或者：菜单 → 更多工具 → 扩展程序

2. 找到"即时翻译"扩展

3. 点击**刷新图标**🔄重新加载扩展

4. 确保**开发者模式**已开启（右上角开关）

### 2. 测试引擎下拉框

**打开方式 1：Popup 模式**
1. 点击浏览器工具栏的即时翻译图标
2. 查看 Header 右侧是否有**引擎选择下拉框**
3. 下拉框应该显示：
   - 谷歌翻译引擎
   - AI 智能引擎

**打开方式 2：侧边栏模式**
1. 按快捷键 `Alt + Q`（Mac 上是 `Option + Q`）
2. 或者右键点击扩展图标 → 选择"打开侧边栏"
3. 查看侧边栏中是否有引擎下拉框

**测试功能：**
- [ ] 下拉框可以正常打开
- [ ] 可以选择不同的引擎
- [ ] 切换引擎后设置自动保存
- [ ] 如果有输入文本，切换引擎后自动重新翻译

### 3. 测试拖拽功能

**Popup 模式：**
1. 点击扩展图标打开 popup
2. 鼠标悬停在 Header 最左端的六圆点图标上
3. 光标应该变为抓取状态 (👆)
4. 按住拖拽图标移动鼠标
5. 注意：Popup 模式下拖拽效果有限，主要保存偏移量

**悬浮窗模式：**
1. 在网页上按 `Alt + Q` 打开侧边栏
2. 悬浮窗应该出现在页面右下角
3. 按住 Header 拖动到任意位置
4. 释放鼠标
5. 关闭悬浮窗（点击 ×）
6. 重新打开，检查是否恢复到上次的位置

### 4. 测试宽度调整

**在 popup 或悬浮窗中：**
1. 点击 Header 最右端的宽度调整按钮（↔图标）
2. 观察宽度变化：
   - 第 1 次点击：宽度变为屏幕的 50%
   - 第 2 次点击：宽度变为屏幕的 33.3%
   - 第 3 次点击：恢复默认宽度 400px

3. 检查工具提示（hover 时显示的文字）：
   - 默认：`调整宽度（点击切换为 50%）`
   - 50%：`宽度：50%（点击切换为 33%）`
   - 33%：`宽度：33%（点击恢复默认）`

4. 关闭并重新打开，检查宽度是否恢复

### 5. 测试侧边栏模式

**打开侧边栏的方法：**

方法 1：快捷键
- Windows/Mac：`Alt + Q`

方法 2：右键菜单
- 右键点击扩展图标
- 选择"打开侧边栏"

方法 3：点击图标
- 直接点击扩展图标（如果配置为打开侧边栏）

**侧边栏应该显示：**
- ✅ Header 区域（包含 Logo、拖拽图标、引擎下拉框、宽度调整按钮）
- ✅ Tab 切换（文本翻译、网页滑词翻译、网页对照翻译）
- ✅ 翻译输入和输出区域
- ✅ 所有功能按钮

---

## ❌ 常见问题排查

### 问题 1：引擎下拉框不显示

**可能原因：**
1. 扩展没有重新加载
2. 浏览器缓存了旧版本
3. JavaScript 报错

**解决方法：**
1. 在扩展管理页面重新加载扩展
2. 清除浏览器缓存（`Ctrl + Shift + Delete`）
3. 打开开发者工具（F12）查看 Console 是否有错误

### 问题 2：侧边栏打不开

**可能原因：**
1. 浏览器版本不支持 sidePanel
2. manifest.json 配置问题
3. 权限不足

**解决方法：**
1. 确保使用 Chrome 114+ 或 Edge 114+
2. 检查 `chrome://extensions/` 中扩展是否启用
3. 查看 manifest.json 中是否有 `sidePanel` 权限

### 问题 3：拖拽不生效

**Popup 模式：**
- 这是正常的！Popup 的位置由浏览器控制，无法自由移动
- 拖拽功能主要在悬浮窗模式下生效

**悬浮窗模式：**
1. 确保按住的是 Header 区域（不是按钮或下拉框）
2. 检查 Console 是否有 JavaScript 错误
3. 重新加载扩展

### 问题 4：宽度调整不生效

**检查项：**
1. Console 是否有错误
2. 是否收到了 `jt_width_changed` 消息
3. background.js 是否正确转发消息

**调试方法：**
打开开发者工具 Console，输入：
```javascript
chrome.storage.local.get(['widthState'], (res) => {
  console.log('当前宽度状态:', res.widthState);
});
```

---

## 🐛 如果问题仍然存在

### 1. 查看错误日志

打开开发者工具（F12）：
1. 切换到 **Console** 标签
2. 查看是否有红色错误信息
3. 截图保存错误信息

### 2. 检查扩展版本

在 `chrome://extensions/` 页面：
1. 确认扩展版本是 **1.2** 或更高
2. 确认扩展已启用
3. 确认"允许访问文件网址"已开启（如果需要）

### 3. 完全重新安装

如果以上方法都无效：

1. **卸载扩展：**
   - 在 `chrome://extensions/` 页面点击"移除"

2. **清理缓存：**
   - 关闭所有浏览器窗口
   - 重新打开浏览器

3. **重新安装：**
   - 打开 `chrome://extensions/`
   - 开启"开发者模式"
   - 点击"加载已解压的扩展程序"
   - 选择 `/Users/apple/Desktop/即时翻译` 文件夹

4. **测试功能**

---

## ✅ 成功标准

所有功能正常工作时，应该看到：

### Popup / 侧边栏
```
┌──────────────────────────────────────┐
│ [⋮⋮] 🌐 即时翻译    [谷歌翻译▼] [↔]   │
├──────────────────────────────────────┤
│ [文本翻译] [网页滑词翻译] [网页对照]   │
├──────────────────────────────────────┤
│                                      │
│   翻译输入和输出区域...               │
│                                      │
└──────────────────────────────────────┘
```

### 悬浮窗（在网页上）
```
┌──────────────────────────────────────┐
│ [⋮⋮] 🌐 即时翻译    [谷歌翻译▼] [↔] [×]│
├──────────────────────────────────────┤
│                                      │
│   iframe 内容（popup.html）           │
│                                      │
└──────────────────────────────────────┘
```

---

## 📞 需要帮助？

如果遇到问题，请提供：
1. 浏览器版本（`chrome://version/`）
2. 扩展版本
3. Console 错误截图
4. 问题描述和复现步骤

**文档版本：** v1.0
**更新时间：** 2026-03-11
