# 悬浮窗使用指南

## 🎯 悬浮窗样式更新

悬浮窗现在采用类似"好记性"应用的简洁设计，直接悬浮在页面上。

---

## 📐 悬浮窗结构

```
┌─────────────────────────────────┐
│ [⋮⋮] 🌐 即时翻译  [引擎▼] [↔] [×] │ ← Header (44px 高)
├─────────────────────────────────┤
│                                 │
│         iframe 内容区域          │
│        (加载 popup.html)         │
│                                 │
│                                 │
│                                 │
│                                 │
└─────────────────────────────────┘
```

### Header 区域（从左到右）

1. **拖拽手柄** (⋮⋮) - 20x20px
   - 用于拖拽悬浮窗
   - 灰色六圆点图标

2. **Logo 和品牌名**
   - 18x18px 图标
   - "即时翻译" 文字

3. **引擎选择下拉框**
   - 白色背景，带边框
   - 选项：谷歌翻译、AI 引擎

4. **宽度调整按钮** (↔) - 28x28px
   - 点击循环切换宽度
   - 默认 → 50% → 33% → 默认

5. **关闭按钮** (×) - 28x28px
   - 点击关闭悬浮窗

---

## 🎨 设计特点

### 尺寸
- **默认宽度**: 320px
- **50% 宽度**: 屏幕宽度的 50%（最大 600px）
- **33% 宽度**: 屏幕宽度的 33%（最大 400px）
- **高度**: 480px（可调整）
- **Header 高度**: 44px

### 位置
- **默认位置**: 距离顶部 100px，距离右侧 100px
- **可拖拽**: 可以移动到任意位置
- **位置记忆**: 自动保存位置，下次打开时恢复

### 样式
- **背景**: 白色 (#ffffff)
- **圆角**: 12px
- **阴影**: 柔和的多层阴影
- **边框**: 浅灰色 (#e5e7eb)

---

## 🚀 如何打开悬浮窗

### 方法 1：快捷键
按 `Alt + Q`（Mac 上是 `Option + Q`）

### 方法 2：右键菜单
1. 右键点击浏览器工具栏的即时翻译图标
2. 选择"打开侧边栏"或相关选项

### 方法 3：自动打开
如果设置了自动打开，页面加载时会自动显示悬浮窗

---

## 📝 使用技巧

### 1. 拖拽移动
1. 鼠标悬停在 Header 区域（灰色区域）
2. 光标变为抓取状态 (👆)
3. 按住拖拽到任意位置
4. 释放鼠标

### 2. 调整宽度
1. 点击宽度调整按钮 (↔)
2. 宽度循环变化：
   - 第 1 次点击：50% 屏幕宽度
   - 第 2 次点击：33% 屏幕宽度
   - 第 3 次点击：恢复默认 320px

### 3. 选择引擎
1. 点击引擎下拉框
2. 选择：
   - **谷歌翻译**: 使用 Google 翻译 API
   - **AI 引擎**: 使用 AI 智能翻译

### 4. 关闭悬浮窗
点击右上角的 × 按钮

---

## 💾 自动保存

悬浮窗会自动保存以下信息：

- ✅ **位置** (panelX, panelY)
- ✅ **宽度状态** (widthState)
- ✅ **打开状态** (floatingPanelOpen)
- ✅ **引擎选择** (engine)

下次打开时会自动恢复上次的状态。

---

## 🔧 技术细节

### 悬浮窗层级
- `z-index: 2147483647` (最高层级)
- 确保在所有网页元素之上

### iframe 内容
- 加载 `popup.html`
- 完全独立的沙箱环境
- 与 popup 模式共享同一套 UI

### 边界检测
拖拽时会自动检测边界：
- 不会超出浏览器左/上边界（最小 0px）
- 不会超出浏览器右/下边界（最大 screenWidth - panelWidth）

---

## 🎯 与旧版本的区别

### 旧版本（侧边栏模式）
- ❌ 固定在浏览器右侧
- ❌ 占用整个侧边空间
- ❌ 无法自由移动
- ❌ 样式复杂

### 新版本（悬浮窗模式）✅
- ✅ 自由悬浮在页面上
- ✅ 紧凑简洁的设计
- ✅ 可以拖拽到任意位置
- ✅ 类似便签应用的样式
- ✅ 不遮挡页面主要内容
- ✅ 更容易融入阅读流程

---

## 📱 响应式设计

### 小屏幕适配
- 最大宽度限制为 80vw（屏幕宽度的 80%）
- 最大高度限制为 80vh（屏幕高度的 80%）
- 确保在小屏幕上也能完整显示

### 多显示器支持
- 位置信息绝对定位
- 切换显示器时保持相对位置

---

## 🐛 常见问题

### Q: 悬浮窗打不开？
**A:** 
1. 检查扩展是否已重新加载
2. 查看 Console 是否有错误
3. 确保页面不是 chrome:// 开头的特殊页面

### Q: 拖拽不生效？
**A:**
1. 确保按住的是 Header 区域（不是按钮）
2. 检查是否在其他元素上释放了鼠标
3. 刷新页面重试

### Q: 位置没有保存？
**A:**
1. 检查 chrome.storage.local 权限
2. 确保正常关闭悬浮窗（点击 ×）
3. 不要直接刷新页面

### Q: 宽度调整没反应？
**A:**
1. 检查 Console 是否有错误
2. 确保点击的是宽度调整按钮 (↔)
3. 等待 0.3s 的动画过渡完成

---

## 🎨 自定义样式（高级）

如果你想自定义悬浮窗样式，可以修改 `content.js` 中的以下部分：

```javascript
// 修改默认位置
floatingPanel.style.cssText = `
  position: fixed;
  z-index: 2147483647;
  top: 100px;      // 修改顶部距离
  right: 100px;    // 修改右侧距离
  width: 320px;    // 修改默认宽度
  // ...
`;

// 修改 Header 样式
header.style.cssText = `
  // 修改背景颜色
  background: #f9fafb;  // 改为 #ffffff 或其他颜色
  // 修改高度
  height: 44px;  // 改为其他值
  // ...
`;
```

---

## 📞 需要帮助？

如有问题，请查看：
1. Console 错误日志（F12 → Console）
2. 扩展管理页面（chrome://extensions/）
3. 本文档的常见问题部分

**文档版本**: v2.0
**更新日期**: 2026-03-11
